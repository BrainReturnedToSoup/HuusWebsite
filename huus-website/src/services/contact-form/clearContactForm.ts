import { store, selectors } from "../../state/store";

export default function clearContactForm_binded() {
    clearContactForm(store, selectors);
}

function clearContactForm(store, selectors) { 
    //logic here

}