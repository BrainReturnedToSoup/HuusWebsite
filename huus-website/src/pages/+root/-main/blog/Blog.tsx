import Regular from "./regular/Regular";
import Mobile from "./mobile/Mobile";

interface BlogProps {
  screenWidth: number;
}

export default function Blog({ screenWidth }: BlogProps) {
  return <>{}</>;
}
